export const courseMap: Record<string, string> = {
  // Example: replace this with your real UUID
  "test-course": "SMA Private Pilot Training Course",
  // add more courseId → courseName pairs as needed
};
